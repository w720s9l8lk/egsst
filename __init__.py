## 这是gt4dvs/文件夹的__init__.py

# from .datasets import datasets:这种导入方式表示从当前包(即包含该代码文件的目录)中导入 datasets 模块
# from . import datasets:这种导入方式表示从当前包中导入所有以 datasets 开头的模块。这里的 . 同样表示当前包，因此这种导入方式会将当前包中所有以 datasets 开头的模块都导入进来，包括子包中的同名模块。如果当前包中有多个以 datasets 开头的模块，那么这些模块都会被导入进来。
# * 在.后面加名称用于避免命名冲突


# from .datasets import *
